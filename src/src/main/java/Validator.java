/**
 * Класс, отвечающий за валидацию входных данных, таких как:
 *  - существование файла
 *  - допустимость ключа
 */

public class Validator {

    public boolean isValidKey(int key, char[] alphabet) {
        // Проверка ключа
        return true;
    }

    public boolean isFileExists(String filePath) {
        // Проверка существования файла
        return true;
    }
}
